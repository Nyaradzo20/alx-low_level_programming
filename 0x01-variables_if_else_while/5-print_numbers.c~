#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
  int num;
  for (num = 0 ; num <= 9 ; num++)
    printf("%d", num);
  puts("");
  return (0);
}
#include <stdio.h>
/**
 * main - print int
 *
 *Return - (0)
 */
int main(void)
{
int a;
for (a = 0 ; a <= 9 ; a++)
printf("%d", a);
puts("");
return (0);
}
