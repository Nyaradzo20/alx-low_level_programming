#include "main.h
/**
 * main - check the code
 *
 * Return: Always 0.
 */
void rev_string(char *s)
{
  int length = 0;
  while (s[length] != '\0')
    {
      length ++;
    }
  return (length - 1);
    }
