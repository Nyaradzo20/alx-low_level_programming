#include "main.h"
/**
 *
 *
 *
 **/
void more_numbers(void)
{
  int row;
  int num;
  for (row = 0; row <= 9; row++)
  { 
    for (num = 0; num <= 13; num++)
      {
	_putchar(num);
      }
    {
      _putchar('\n');
    }
  }
}
